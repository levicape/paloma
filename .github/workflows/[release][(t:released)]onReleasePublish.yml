# THIS FILE WAS AUTOMATICALLY GENERATED, DO NOT MODIFY
# .../codegen/github/GenerateGithubNode.mjs
########################################
name: "on Release: [released] Publish @levicape Github"
on:
  release:
    types:
      - released
env:
  NPM_REGISTRY_PROTOCOL: https
  NPM_REGISTRY_HOST: npm.pkg.github.com
jobs:
  packages:
    name: Compile package, test and publish to npm
    permissions:
      packages: write
      contents: read
    runs-on: ubuntu-latest
    steps:
      - name: Verify registry URL
        run: |-
          echo "NPM_REGISTRY_URL: ${{ env.NPM_REGISTRY_PROTOCOL }}://${{ env.NPM_REGISTRY_HOST }}";
          curl -v --insecure ${{ env.NPM_REGISTRY_PROTOCOL }}://${{ env.NPM_REGISTRY_HOST }};
        continue-on-error: true
      - name: Checkout
        uses: actions/checkout@v2
      - name: Setup pnpm
        uses: pnpm/action-setup@v4
      - name: Setup Node 22.12.0
        uses: actions/setup-node@v4
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          node-version: 22.12.0
          registry-url: ${{ env.NPM_REGISTRY_PROTOCOL }}://${{ env.NPM_REGISTRY_HOST }}/
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml
          scope: "@levicape"
      - id: cache-npm
        name: Cache node modules
        uses: actions/cache@v3
        env:
          cache-name: cache-node-modules
        with:
          path: ~/.npm
          key: ${{ runner.os }}-build-${{ env.cache-name }}-${{ hashFiles('**/pnpm-lock.json') }}
          restore-keys: |-
            ${{ runner.os }}-build-${{ env.cache-name }}-
            ${{ runner.os }}-build-
            ${{ runner.os }}-
      - if: ${{ steps.cache-npm.outputs.cache-hit != 'true' }}
        name: List Dependencies
        run: pnpm list;
        continue-on-error: true
      - name: Install Dependencies
        run: pnpm install;
      - name: Verify registry URL
        run: |-
          echo "NPM_REGISTRY_URL: ${{ env.NPM_REGISTRY_PROTOCOL }}://${{ env.NPM_REGISTRY_HOST }}";
          echo "NPM_REGISTRY_SCOPE: @levicape";
          curl -v --insecure ${{ env.NPM_REGISTRY_PROTOCOL }}://${{ env.NPM_REGISTRY_HOST }};
        continue-on-error: true
      - name: Compile module
        run: pnpm run compile;
      - name: "Run package.json: lint"
        run: pnpm run lint;
      - name: "Run package.json: test"
        run: pnpm run test;
      - name: Increment version
        env:
          RELEVANT_SHA: ${{ github.event.release.target_commitish || github.sha }}
        run: |-
          export PREID=$RELEVANT_SHA;
          export PREID=${PREID:0:10};
          export ARGS="--git-tag-version=false --commit-hooks=false";
          npm version ${{ github.event.release.tag_name }}-$PREID.${{ github.run_number }} $ARGS --allow-same-version;
      - name: "Run package.json: prepublish"
        run: pnpm run prepublish;
      - if: success()
        name: Increment version
        run: pnpm publish --no-git-checks;
        continue-on-error: true

########################################
########################################
# {"started":"2024-12-14T06:55:35.967Z","now":"2024-12-14T06:55:35.978Z","elapsed":"11ms","hashed":"10aaa9c572b16450e08fc6ae52877bd4ed242cc274d2317e27cdc3d6d534488b","signed":""}
# END OF GENERATED FILE
